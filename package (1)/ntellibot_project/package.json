{
  "name": "ntellibot-osint-cloudflare",
  "version": "2.0.0",
  "description": "!ntellibot OSINT Dashboard - Migrated to Cloudflare Pages",
  "main": "functions/_worker.js",
  "type": "module",
  "scripts": {
    "deploy": "npx wrangler pages deploy ./ --project-name=ntellibot-osint",
    "dev": "npx wrangler pages dev ./ --live-reload",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "osint",
    "intelligence",
    "dashboard",
    "cloudflare-pages",
    "geopolitical"
  ],
  "author": "!ntellibot",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
